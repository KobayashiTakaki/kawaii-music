<h1>Tracks</h1>
<%= link_to 'new', new_admin_track_path %>
<div class="tracks-list">
  <ul>
    <% @tracks.each do |track| %>
      <li>
        <%= link_to "#{track.artist} - #{track.title}", track.url %>
      </li>
    <% end %>
  </ul>
</div>
