var html = "<%= escape_javascript(render partial: 'tracks/article', collection: @tracks, as: 'track')%>";
$("#articles").append($(html));
if(parseInt("<%= @total_pages %>") <= parseInt("<%= @page %>")){
  $("#show-more").remove();
}
